<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.swxz.mapper.LostItemMapper">
    
    <select id="searchLostItems" resultType="com.example.swxz.entity.LostItem">
        SELECT * FROM lost_items
        <where>
            is_claimed = 0
            <if test="param.type != null">
                AND type = #{param.type}
            </if>
            <if test="param.color != null">
                AND color = #{param.color}
            </if>
            <if test="param.name != null and param.name != ''">
                AND name LIKE CONCAT('%', #{param.name}, '%')
            </if>
            <if test="param.building != null">
                AND building = #{param.building}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>
    
    <select id="findByAdminId" resultType="com.example.swxz.entity.LostItem">
        SELECT * FROM lost_items
        WHERE admin_id = #{adminId} AND is_claimed = 0
        ORDER BY create_time DESC
    </select>
    
</mapper>